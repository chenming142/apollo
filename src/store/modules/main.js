const state = {
  sensitiveWords: []
};
const getters = {
  sensitiveWords: (state) => state.sensitiveWords, 
};
const actions = {};
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
}